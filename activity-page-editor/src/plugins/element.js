import Vue from 'vue'
import '../../theme/index.css'
import { Button, Container, Header, Aside, Main, Form, FormItem, Input, ColorPicker, Upload, Dialog, Tooltip, Table, TableColumn, Image } from 'element-ui'

Vue.use(Button)
  .use(Container).use(Header).use(Aside).use(Main)
  .use(Form).use(FormItem).use(Input).use(ColorPicker).use(Upload)
  .use(Dialog)
  .use(Tooltip)
  .use(Table).use(TableColumn)
  .use(Image)
